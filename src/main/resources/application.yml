spring:
  datasource:
    url: jdbc:postgresql://localhost:5333/postgres
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true